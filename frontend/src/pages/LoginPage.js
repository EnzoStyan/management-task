import React from 'react';

function LoginPage() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <h1 className="text-2xl">Login Page</h1>
      {/* Form login akan dibuat di sini */}
    </div>
  );
}

export default LoginPage;